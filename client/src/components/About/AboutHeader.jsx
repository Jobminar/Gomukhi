import React from "react";
import { Container } from "react-bootstrap";
import "./style/AboutHeader.css"; // Ensure this is the correct path to your CSS file

const AboutHeader = () => {
  return <Container fluid className="about-header"></Container>;
};

export default AboutHeader;
